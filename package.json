{
    "name": "loyalty-pwa-monorepo",
    "version": "0.1.0",
    "private": true,
    "scripts": {
        "lint": "pnpm -r lint",
        "typecheck": "pnpm -r typecheck",
        "format": "prettier --write ."
    },
    "devDependencies": {
        "prettier": "^3.2.5",
        "eslint": "^8.56.0"
    },
    "engines": {
        "node": ">=20.0.0"
    }
}